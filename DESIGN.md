# Media Agent · Web UI 重新设计方案

> 本文档描述了 Media Agent Web 界面的全新设计方向。review 并批准后进入实现阶段。

---

## 1. 设计理念

### 1.1 为什么重新设计

当前 UI 功能完整但视觉上过于「工具化」——表格堆叠、内容密集、缺乏层次感。作为一款面向**自媒体创作者**的内容生产工具，界面本身应该传递**品质感**和**创作氛围**，而不是像后台管理面板。

### 1.2 美学方向：Warm Studio Dark

选定的美学方向是 **Warm Studio Dark** —— 一间温暖、幽暗的创意工作室/控制室。

| 维度 | 定位 |
|------|------|
| **调性** | 专业但不冰冷，有温度的工具感 |
| **色温** | 暖暗色（非冷蓝黑），类似发电站控制室或编辑室 |
| **气质** | 克制、精致、内容优先 |
| **差异化** | 大多数工具 UI 用冷色科技感，我们反向走暖色编辑室风格 |

### 1.3 三个设计目标

1. **内容呼吸** —— 给文章、草稿、内容本身更多视觉权重，而非表格线框
2. **层次分明** —— 用 typography + 空间代替边框分割信息层级
3. **细腻动效** —— 微交互动效让界面有生命感，但不喧宾夺主

---

## 2. 设计系统

### 2.1 色彩

采用暖色调深色基底，琥珀金为主色（呼应媒体/出版业传统），翡翠绿为完成态。

```css
:root {
  /* ── 基础色 ── */
  --bg:        #0d0d0d;   /* 近黑暖底 */
  --surface:   #161618;   /* 面板 / 卡片 */
  --surface2:  #1e1e22;   /* 悬浮 / 次级面板 */
  --border:    #2c2c30;   /* 分割线 */

  /* ── 文字 ── */
  --text:      #f0efec;   /* 暖白主文字 */
  --text2:     #a8a6a0;   /* 辅助文字 */
  --text3:     #6b6a66;   /* 占位 / 禁用 */

  /* ── 强调色（暖色编辑室） ── */
  --amber:     #f59e0b;
  --amber-dim: #b45309;
  --emerald:   #10b981;
  --indigo:    #6366f1;
  --rose:      #f43f5e;

  /* ── 语义色 ── */
  --success:   var(--emerald);
  --warning:   var(--amber);
  --danger:    var(--rose);
  --info:      var(--indigo);
}
```

> **为什么不用冷蓝/紫色？** 大多数 SaaS 工具使用蓝紫色系（科技感）。暖色在暗底上更舒适、更「可读」，也更适合长时间使用的内容工具。

### 2.2 排版

| 层级 | 字体 | 字重 | 字号 | 行高 |
|------|------|------|------|------|
| **品牌/大标题** | `"Noto Serif SC", "Source Han Serif SC", serif` | 700 | 28px / 24px | 1.3 |
| **页面标题** | `"PingFang SC", "Hiragino Sans GB", system-ui, sans-serif` | 600 | 22px | 1.4 |
| **卡片/小节标题** | 同上 | 600 | 16px | 1.4 |
| **正文** | 同上 | 400 | 14px | 1.6 |
| **辅助/标签** | 同上 | 400 | 12px | 1.4 |
| **代码/路径** | `"JetBrains Mono", "SF Mono", "Cascadia Code", monospace` | 400 | 13px | 1.5 |

- 衬线体用于品牌/大标题，传递**编辑/出版/媒体**气质
- 正文无衬线保证中文阅读体验，`font-weight` 在 400–600 之间，不加粗
- 自动探测系统字体回退

### 2.3 间距与布局

```
--space-1:  4px
--space-2:  8px
--space-3:  12px
--space-4:  16px
--space-5:  24px
--space-6:  32px
--space-8:  48px
--space-10: 64px
```

- 页面最大宽度 `1120px`，两侧留白 `24px`
- 卡片内边距统一 `20px`，圆角 `12px`
- 垂直间距使用 8px 倍率

### 2.4 圆角与阴影

```css
--radius-sm:  6px;
--radius-md:  10px;
--radius-lg:  16px;
--radius-xl:  24px;

--shadow-sm:  0 1px 2px rgba(0,0,0,.3);
--shadow-md:  0 4px 12px rgba(0,0,0,.4);
--shadow-lg:  0 8px 30px rgba(0,0,0,.5);
```

### 2.5 动效原则

- **入场**：页面/卡片/面板从下往上 + 淡入，`0.3s ease-out`
- **悬浮**：亮度/边框微变，`0.15s ease`
- **切换**（Tab/展开）：`0.2s ease`
- **状态变化**（运行中/完成）：平滑过渡，避免闪烁
- 所有动效使用 `opacity` + `transform`，触发 GPU 合成

---

## 3. 页面级设计

### 3.1 全局布局（base.html）

```
┌────────────────────────────────────────────────────┐
│ ██  Logo         导航 [仪表盘 归档 草稿 来源 设置] │
│                                         [▶ 立即运行]│
├────────────────────────────────────────────────────┤
│                                                    │
│                  页面内容 (container)               │
│                                                    │
├────────────────────────────────────────────────────┤
│   [浮动] 运行进度面板 (右下角，可拖拽)              │
└────────────────────────────────────────────────────┘
```

**顶部导航变化：**
- 品牌区：Logo + 「Media Agent」衬线体，加微弱 glow 效果
- 导航项：悬停有底部渐变线（amber），激活态加 dot indicator
- 「立即运行」按钮：琥珀色实心，hover 有光晕

**运行进度面板：**
- 右下角固定，折叠态只显示状态标签
- 展开显示实时日志，可拖拽位置
- 运行结束时呼吸光效（绿/红）

### 3.2 仪表盘

当前问题：简单堆叠三个数字卡片 + 两个表格。

**新设计：**

```
┌─────────────────────────────────────────────┐
│ 仪表盘                                       │
│                                             │
│ ┌──────┐ ┌──────┐ ┌──────┐                  │
│ │ 28   │ │ 12   │ │ 47   │                  │
│ │ 文章  │ │ 草稿  │ │ 运行  │                  │
│ └──────┘ └──────┘ └──────┘                  │
│                                             │
│ 最近运行 (时间线)                             │
│ ┌─────────────────────────────────────────┐ │
│ │ ● 2026-06-28 08:00  成功  8 篇新归档     │ │
│ │ ● 2026-06-27 20:00  失败  LLM 超时      │ │
│ │ ● 2026-06-27 08:00  成功  12 篇新归档    │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 最近草稿                                     │
│ ┌──────────────┐ ┌──────────────┐           │
│ │ #12 AI 综述   │ │ #11 机器人   │           │
│ │ 转写完成      │ │ 审核中       │           │
│ └──────────────┘ └──────────────┘           │
└─────────────────────────────────────────────┘
```

**变化要点：**
- 统计卡片加 icon、微光效、数字 `font-variant-numeric: tabular-nums` 等宽对齐
- 运行记录改为时间线（点 + 线 + 内容），比表格更有叙事感
- 草稿卡片化，显示状态标签 + 更新时间，点击跳转编辑
- 「危险操作」聚合在折叠区域，减少视觉压迫

### 3.3 归档浏览

当前问题：表格行很多，功能按钮密集，操作区拥挤。

**新设计：**

```
┌─────────────────────────────────────────────┐
│ 归档浏览                                     │
│                                             │
│ [全部] [AI] [机器人] [科技] [+主题筛选]      │
│                                             │
│ [展开全部] [收起全部]                        │
│                                             │
│ ▸ 今天 · 5 篇                               │
│ ┌─ 文章卡片 ──────────────────────────────┐ │
│ │ GPT-5 发布对行业的影响                     │ │
│ │ OpenAI Blog  ·  AI  ·  2026-06-28       │ │
│ │ [查看] [重新抓取] [本地化] [转写 ✓]       │ │
│ └─────────────────────────────────────────┘ │
│ ┌─ 文章卡片 ──────────────────────────────┐ │
│ │ ...                                      │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ▸ 昨天 · 3 篇                               │
│ ...                                         │
└─────────────────────────────────────────────┘
```

**变化要点：**
- **表格 → 文章卡片**：每篇文章是一张紧凑卡片，标题突出，元数据次之，操作按钮右对齐
- 日期分组保留但视觉加强（带计数徽标，可展开收起）
- 操作按钮统一为 icon + 文字或仅 icon（hover 显示 tooltip），节省空间
- 主题筛选 chip 改为 pill 样式，选中高亮渐变
- 「转写/重写/草稿」状态用 emerald/amber 徽标，一目了然
- 浮动任务面板（重新抓取/本地化）保留右下角定位

### 3.4 草稿列表

当前问题：纯表格，信息密度低。

**新设计：**

```
┌─────────────────────────────────────────────┐
│ 草稿                                         │
│                                             │
│ [全部] [drafted] [reviewing] [approved]     │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ #28  审核中  公众号   AI 行业趋势分析     │ │
│ │      2026-06-28 14:30  → 编辑           │ │
│ ├─────────────────────────────────────────┤ │
│ │ #27  已完成  知乎     机器人伦理探讨     │ │
│ │      2026-06-27 09:15  → 编辑           │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

**变化要点：**
- **表格 → 列表行卡片**：每行是独立卡片，状态 + 平台 + 标题层次分明
- 状态用彩色圆点 + 标签，平台用对应颜色标签
- 路径显示在 hover 或折叠区域

### 3.5 草稿编辑

当前最复杂的页面，保持功能不变但优化交互：

**文章内容 Tab：**
- 左侧元数据区（封面、来源、校验警告）改为可折叠 sidebar
- EasyMDE 编辑区占据主要空间
- 警告框（事实校验 + 敏感词）用醒目标签 + 折叠详情
- 工具栏固定（状态/保存/平台适配/返回）
- 平台适配按钮改为 dropdown 或分组

**讲解视频 Tab：**
- 改进分镜卡片设计：更清晰的序号、拖拽排序手柄（未来）、视觉化媒体选择
- 状态/进度区域统一
- 视频预览更突出

### 3.6 来源管理

当前页面内容最丰富（智能推荐 + 热度排名 + 主题 + 来源 + 发现），重新分区：

```
┌─────────────────────────────────────────────┐
│ 来源与主题                                    │
│                                             │
│ ┌─ 智能推荐主题 ──────────────────────────┐ │
│ │ 输入主题 → 推荐子主题 → 推荐关键词        │ │
│ │ 交互流程不变，UI 优化                     │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ┌─ 热度排名 ──────────────────────────────┐ │
│ │ 双列布局：主题热度 + 来源热度            │ │
│ │ 条形图更美观                             │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 主题                                         │
│ ┌─ 主题卡片 ──────────────────────────────┐ │
│ │ AI · 关键词: LLM, GPT, model            │ │
│ │ [编辑] [发现来源] [删除]                │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 来源                                         │
│ ┌─ 来源卡片 ──────────────────────────────┐ │
│ │ OpenAI Blog · rss · AI                   │ │
│ │ [编辑] [启用/禁用] [删除]               │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 手动发现来源                                 │
│ [从网址发现____] [按关键词发现____]          │
└─────────────────────────────────────────────┘
```

**变化要点：**
- **表格 → 卡片/列表**：主题和来源从表格改为紧凑卡片，更易批量操作
- 智能推荐区域视觉优化，分步引导更清晰
- 热度排名条加渐变色
- 批量操作复选框保留，但用更现代的 checkbox 样式

### 3.7 设置

**变化要点：**
- 字段分组用侧边小标题 + 内容区，而非传统 fieldset（类似现代 app 设置页）
- API Key 输入区更安全（默认 mask，点按才显示）
- 声音库录制面板保持功能，视觉更新
- 每节末尾「保存」改为页面级浮动保存按钮
- 导出/导入合并到页面底部

---

## 4. 组件级设计

### 4.1 按钮系统

| 类型 | 用途 | 样式 |
|------|------|------|
| `primary` | 主要操作（运行、保存、转写） | 实心 amber，hover 微光晕 |
| `secondary` | 次要操作（编辑、返回） | 边框线，透明底 |
| `ghost` | 极轻操作（查看、取消） | 只有文字，hover 显示背景 |
| `danger` | 危险操作（删除、清空） | 实心 rose |
| `small` | 紧凑场景 | 字号 12px，padding 6px 10px |

### 4.2 表单元素

- 输入框：深色底 `var(--surface2)`，聚焦时边框 amber + 微光晕
- Select：自定义下拉箭头，统一圆角
- Checkbox/Radio：自定义样式（替换原生），方形勾选/圆形选中
- 文本区域：与输入框一致，深色底，focus 光晕

### 4.3 标签（Badge / Chip）

- Badge：圆角 999px，12px 字号，用于状态
- Chip：可交互的 Badge，hover 高亮，选中渐变填充

### 4.4 警告/提示框

- Success: 左边框 emerald + 浅绿底
- Warning: 左边框 amber + 浅琥珀底
- Error: 左边框 rose + 浅玫红底
- Info: 左边框 indigo + 浅紫底

### 4.5 空状态

- 不再仅显示 `暂无文章` 文字
- 配简洁插图/icon + 引导文案 + 操作按钮

---

## 5. 动效细节

### 5.1 页面加载

- 页面内容块逐个 `fade-in-up`，间隔 `80ms` 交错入场
- 顶部导航 `slide-down` 入场
- 运行面板 `slide-up` 从底部出现

### 5.2 悬浮反馈

- 卡片 hover：`translateY(-2px)` + 阴影增强，150ms
- 按钮 hover：`brightness(1.08)`，active: `scale(0.97)`
- 链接 hover：`color` 过渡 + 微下划线

### 5.3 状态变化

- 运行状态轮询：新日志行从下往上滑入
- 切换 Tab：内容块 `fade-in`
- 展开/收起（date-group）：图标旋转 `90deg`，内容 `max-height` 过渡

### 5.4 提醒/通知

- 浮动提示（toast）从右上角滑入，自动消失
- 关键操作（保存/转写成功）触发的 toast

---

## 6. 交互设计规范（重点）

### 6.1 按钮状态体系

每个按钮必须覆盖以下状态：

| 状态 | 触发 | 视觉表现 | 对应场景 |
|------|------|----------|----------|
| **default** | 页面加载/就绪 | 正常样式 | 所有按钮初始态 |
| **hover** | 鼠标悬停 | `filter: brightness(1.1)` + 微 shadow 上浮 | — |
| **active** | 按下时 | `transform: scale(0.97)` | — |
| **loading** | 点击后/异步请求中 | 按钮内部显示 spinner（SVG 旋转动画），文字变为「操作名+…」，按钮 `disabled` 防止重复点击 | 保存、转写、运行、生成视频/配音 |
| **success** | 操作成功 | 按钮闪绿（emerald）0.5s → 恢复 default；若触发页面刷新则直接跳转 | 保存设置、转写完成 |
| **error** | 操作失败 | 按钮抖动（shake animation 0.3s）→ 恢复 default + 顶部/侧边 toast 提示错误原因 | 请求超时、服务器错误 |
| **disabled** | 条件不满足 / 冷却中 | `opacity: 0.45` + `cursor: not-allowed`，无 hover 效果 | 选中项为 0 时批量删除、已有运行进行中 |

**loading spinner 规范：**

```css
/* 内联旋转圆环 */
.btn .spinner {
  display: inline-block;
  width: 16px; height: 16px;
  border: 2px solid transparent;
  border-top-color: currentColor;
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
  margin-right: 6px;
  vertical-align: middle;
}
@keyframes spin { to { transform: rotate(360deg); } }
```

> **每一个异步操作按钮都必须实现 loading 态**。用户点了没反应是最大的交互失败。

### 6.2 按钮交互清单（按页面）

| 页面 | 按钮 | 交互行为 |
|------|------|----------|
| **全局** | ▶ 立即运行 | 点击 → 按钮变 loading + 右下角面板滑出 → 面板显示实时日志 → 完成时按钮恢复 + 面板绿色呼吸光或错误红 |
| **全局** | 顶部导航链接 | 点击 → URL 跳转，**当前页面状态通过 URL 参数/ hash 保留** |
| **仪表盘** | 清空归档/草稿/运行 | 点击 → confirm 对话框 → loading → 完成 toast → 刷新计数 |
| **归档** | 转写 / 重写 | 点击 → 单元格内显示 loading → 完成跳转草稿编辑或原地恢复并 toast 错误 |
| **归档** | 重新抓取 / 本地化 | 点击 → 右下角浮动面板显示进度 → 按钮 disabled 防止重复 → 完成后恢复 |
| **归档** | 展开全部/收起全部 | 瞬间切换所有 date-group，无 loading |
| **草稿** | 保存（编辑页） | 点击 → 按钮 loading「保存中…」→ 成功闪绿 0.5s → 恢复 |
| **草稿** | 生成平台适配 | 点击 → 按钮 loading → 完成后 toast 提示 → 如需刷新则跳转 |
| **草稿** | 生成讲解脚本+配音 | 点击 → 按钮 loading + 日志面板展开 → 实时轮询进度 → 完成自动刷新 |
| **草稿** | 合成讲解视频 | 同上 |
| **来源** | 推荐子主题 / 推荐关键词 | 点击 → 按钮 loading → 结果渲染到下方，不影响其他区域 |
| **来源** | 从网址发现 / 按关键词发现 | 点击 → 按钮 loading → 表单提交 → 完成后刷新表格或 toast |
| **设置** | 保存所有设置 | 点击 → 按钮 loading「保存中…」→ 成功闪绿 → toast「已保存，部分设置需重启后生效」 |
| **设置** | 上传录音 / 保存录音 | 点击 → 按钮 loading → 完成后 reload 页面 |

### 6.3 进度显示体系

| 类型 | 实现方式 | 使用场景 |
|------|----------|----------|
| **按钮内 spinner** | SVG 旋转圆环 + 文字 | 单次短操作（保存、转写请求） |
| **行内进度** | 表格单元格/卡片内文字变化 + spinner | 归档每篇文章的独立操作 |
| **浮动面板** | 右下角 panel，显示实时日志（轮询） | 长时间任务（运行流水线、重新抓取、本地化媒体） |
| **Toast 通知** | 右上角滑入，3s 自动消失 | 单次操作完成反馈（保存成功、删除完成） |
| **页面级进度** | 顶部进度条（NProgress 风格） | 页面加载/导航（目前用不到纯前端 SPA 导航，但如果后续引入 htmx 或 turbo 可用） |

**Toast 组件规范：**

```
┌──────────────────────────┐
│ ✓ 设置已保存              │  ← 右上角固定，z-index: 9999
│                          │     success: 左边框 emerald
└──────────────────────────┘     error: 左边框 rose
                                auto-dismiss 3s
                                slide-in-right 0.3s
                                slide-out-right 0.3s
```

> 当前代码中很多操作后直接 `location.reload()` 或无反馈，需要改为 toast + 按需刷新。

### 6.4 状态保持规范

页面切换时，用户的所有临时状态必须保持。具体要求：

| 页面 | 需保持的状态 | 方案 |
|------|-------------|------|
| **归档** | 当前选中的主题筛选 | URL query param `?topic=AI`，刷新/导航回来自动恢复 |
| **归档** | 已展开/收起哪些日期分组 | 用 `localStorage` 或 URL hash 记录，`sessionStorage` 也可 |
| **草稿列表** | 当前选中的状态筛选 | URL query param `?status=reviewing` |
| **草稿编辑** | 当前 Tab（文章/视频） | URL hash `#article` / `#video` ✅ **已有实现，保留** |
| **草稿编辑** | 编辑器光标位置、内容 | 表单内容在切换 Tab 时由浏览器 DOM 维持（隐藏的 tab-pane 不卸载） |
| **来源管理** | 展开的编辑行、推荐中间状态 | 页面内用 JS 状态变量维持，不使用 navigation |
| **设置** | 表单填写内容 | 用户未保存时不应丢失——切换页面再回来应保留（表单状态在 DOM 中维持，Jinja2 服务端渲染 + form value 回填） |
| **所有页面** | 运行面板状态（展开/收起、运行中） | 通过轮询 `/api/run-status` 自动恢复 ✅ **已有实现，保留** |

**通用规则：**
1. **筛选/过滤条件** → URL query params（可分享、可回退、可刷新）
2. **Tab 切换** → URL hash
3. **展开/折叠** → `localStorage` 或 `sessionStorage`
4. **表单填写内容** → 由 DOM 维持（不卸载节点），配合服务端渲染值回填
5. **操作中状态**（loading/进度）→ 不跨页面保持，但如果用户通过历史记录回退，应通过轮询恢复

### 6.5 返回/切换页面时的状态恢复流程

```
用户点击导航 → 浏览器导航到新 URL
   ├─ 新页面渲染（服务端）
   ├─ JS 读取 URL params → 恢复筛选/Tab 状态
   ├─ JS 读取 localStorage → 恢复展开/折叠状态
   ├─ 轮询 API → 恢复运行面板状态
   └─ 内容块交错 fade-in 入场
```

---

## 7. 技术实现方案

### 7.1 约束条件

- 基于现有 **FastAPI + Jinja2 + vanilla CSS/JS**，不引入前端框架
- EasyMDE markdown 编辑器保留（只改样式）
- 所有后端路由不变（只改模板 + CSS + 少量 JS）

### 7.2 CSS 架构

- 单个 `app.css` 重构为新设计系统
- CSS 变量统一管理颜色、间距、圆角
- 类名保持语义化 `.btn` `.card` `.badge` 等，补充新变体
- 所有过渡/动效用 CSS-only 实现

### 7.3 JS 改动最小化

- 现有 inline JS 功能逻辑不动
- 只调整 DOM 类名变化（以匹配新 CSS）
- 新增动效类（如 `.fade-in-up`）通过 CSS animation 实现，无需 JS

### 7.4 实现顺序

| 步骤 | 内容 | 工时预估 |
|------|------|----------|
| 1 | CSS 设计系统变量 + 基础布局重构 (`base.html`, `app.css`) | 高 |
| 2 | 全局导航 + 运行面板重设计 | 中 |
| 3 | 仪表盘重设计 | 低 |
| 4 | 归档页 → 文章卡片化 | 高 |
| 5 | 草稿列表重设计 | 低 |
| 6 | 草稿编辑页（两个 Tab）CSS 优化 + 分镜卡片美化 | 高 |
| 7 | 来源管理页重设计（推荐区 + 表格→卡片） | 高 |
| 8 | 设置页重设计 | 中 |
| 9 | 动效统一 + 状态过渡 | 中 |
| 10 | 跨浏览器/响应式兼容 + 最终检查 | 低 |

---

## 8. 与原设计的对比

| 项目 | 当前 | 新设计 |
|------|------|--------|
| 配色 | 冷深蓝 (#0f1419) + 蓝 accent | 暖深灰 (#0d0d0d) + 琥珀 accent |
| 排版 | system-ui 全统一 | 衬线标题 + 无衬线正文，层次分明 |
| 数据展示 | 以表格为主 | 卡片 + 时间线 + 少量保留表格 |
| 间距 | 紧凑（12-24px） | 层级分明（4-64px 系统） |
| 动效 | 基本无 | 入场 + 悬浮 + 状态过渡 |
| 空状态 | 纯文字 | 插图 + 引导文案 |
| 危险操作 | 直接显示 | 折叠/淡化 |
| 品牌感 | 弱 | Logo + 衬线品牌 + glow 效果 |

---

## 9. 已确认的决策

| 问题 | 决策 |
|------|------|
| 字体 | Noto Serif SC（下载到本地 static/fonts/，不使用 Google CDN） |
| 配色 | 暖色（amber）深色主题 ✅ |
| 数据展示 | **保留表格**（不在归档/草稿/来源页改用卡片，表格信息密度更高） |
| 实现范围 | **一次性全部实现** |
| 用户系统 | 保持纯工具定位，不加用户系统 |

---

> **下一步**：按实现顺序逐步实施。
