feedparser==6.0.11
httpx==0.27.2
trafilatura==1.12.2
readability-lxml==0.8.4.1
PyYAML==6.0.2
python-frontmatter==1.1.0
typer==0.12.5
click>=8.1.7,<8.2
openai==1.54.4
fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.32
pillow==12.2.0
resvg-py==0.3.3
APScheduler==3.10.4
ddgs>=9.0.0              # DuckDuckGo search (image search for scrape-bg)
curl_cffi>=0.7.0          # TLS fingerprint impersonation (bypasses Cloudflare)
json5>=0.9.0              # Lenient JSON parser (handles LLM output violations)
edge-tts>=6.1.0
yt-dlp>=2024.1.1
cryptography==42.0.8      # retains prebuilt Intel macOS wheels; avoids OpenSSL ABI mismatch
pytest==8.3.3

# Optional (install manually; some are heavy):
#   pip install cosyvoice            # CosyVoice2-0.5B local voice cloning (tts_provider=cosyvoice) [torch, CUDA, Python<3.13]
#   pip install playwright && playwright install chromium  # JS rendering (render_js)
